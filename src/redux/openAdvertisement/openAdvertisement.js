const initialState = {
  OpenCards: [],
  loading: false,
}


export const openAdvertisementReducer = (state = initialState, action) => {
  switch(action.type){




    default:
      return state;
  }
}
